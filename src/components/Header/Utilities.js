import React from 'react';

const Utilities = (props) => {
    return (
        <div clasName="Utilities">
            Searchbar
            Login
            Shopping Cart
        </div>
    );
};

export default Utilities;